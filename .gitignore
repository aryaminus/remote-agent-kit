# ── Secrets: never commit these ─────────────────────────────
.env
*.tfvars
*.tfvars.json
*.tfstate
*.tfstate.*
*.backup
terraform/.terraform/
terraform/.terraform.lock.hcl
ansible/inventory/hosts.yml
*.age.key
age-key.txt
hetzner-token.txt
tailscale-authkey.txt

# ── OS / editor ─────────────────────────────────────────────
.DS_Store
Thumbs.db
*~
*.swp
.vscode/
.idea/

# ── Local run history (Gilb's Law logs — yours, never shipped) ───
logs/

# ── Tool caches ─────────────────────────────────────────────
.ansible/
ansible/collections/*
!ansible/collections/requirements.yml
*.retry
